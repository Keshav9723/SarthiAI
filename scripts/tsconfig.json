{
  "//": "Separate tsconfig just for scripts/. The root tsconfig.json excludes",
  "//1": "this directory so Next's production build doesn't type-check it",
  "//2": "(some legacy Cheerio types break strict mode). This file restores IDE",
  "//3": "type-checking + Node globals (process, __dirname, etc.) for editor",
  "//4": "intellisense — Vercel doesn't read this file.",
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["node"],
    "module": "commonjs",
    "moduleResolution": "node",
    "isolatedModules": false
  },
  "include": ["**/*.ts"],
  "exclude": []
}
