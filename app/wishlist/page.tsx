// app/wishlist/page.tsx — saved destinations + itineraries.

import WishlistView from "@/components/wishlist/WishlistView";

export const metadata = {
  title: "Wishlist",
};

export default function WishlistPage() {
  return <WishlistView />;
}
